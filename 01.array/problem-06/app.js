const numbers = [1, 2, 3, 4, 5];
const SquareElement = numbers.map((number) => number * number);
// const SquareElement = numbers.map(function(number){
//    return number * number;
// });
console.log(SquareElement);